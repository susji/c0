int main() {
	for (int z = 0; z < 3; z++)
		printint(z);

	for (int i = 0; i < 10; i++) {
		for (int x = 0; x < 2; x++) {
			if (i > 5) {
				break;
			}
			printint(i);
		}
		println("woohoo almost at 10!");
	}
	int j = 10;
	while (j > 0) {
		j--;
		if (j == 5) {
			println("i stole your five!");
			continue;
		}
		printint(j);
	}
	return 0;
}
